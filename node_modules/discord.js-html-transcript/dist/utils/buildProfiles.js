"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildProfiles = buildProfiles;
const discord_js_1 = require("discord.js");
const utils_1 = require("./utils");
async function buildProfiles(messages) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j;
    const profiles = {};
    const memberCache = new Map();
    const confirmedHumans = new Set();
    const resolveMember = (guild, userId, fallbackMember, isWebhook = false) => {
        if (fallbackMember) {
            return Promise.resolve(fallbackMember);
        }
        if (!guild || isWebhook) {
            return Promise.resolve(null);
        }
        const cacheKey = `${guild.id}:${userId}`;
        const cached = memberCache.get(cacheKey);
        if (cached) {
            return cached;
        }
        const pending = guild.members.fetch(userId).catch(() => null);
        memberCache.set(cacheKey, pending);
        return pending;
    };
    for (const message of messages) {
        void resolveMember((_a = message.guild) !== null && _a !== void 0 ? _a : null, message.author.id, message.member, Boolean(message.webhookId));
        if (message.interaction) {
            void resolveMember((_b = message.guild) !== null && _b !== void 0 ? _b : null, message.interaction.user.id, null, false);
        }
        if ((_c = message.thread) === null || _c === void 0 ? void 0 : _c.lastMessage) {
            void resolveMember((_e = (_d = message.thread.lastMessage.guild) !== null && _d !== void 0 ? _d : message.guild) !== null && _e !== void 0 ? _e : null, message.thread.lastMessage.author.id, message.thread.lastMessage.member, Boolean(message.thread.lastMessage.webhookId));
        }
    }
    // loop through messages
    for (const message of messages) {
        // add all users
        const author = message.author;
        const authorMember = await resolveMember((_f = message.guild) !== null && _f !== void 0 ? _f : null, author.id, message.member, Boolean(message.webhookId));
        profiles[author.id] = mergeProfile(profiles[author.id], buildProfile(authorMember, author));
        if (!author.bot && !message.webhookId) {
            confirmedHumans.add(author.id);
        }
        // add interaction users
        if (message.interaction) {
            const user = message.interaction.user;
            const member = await resolveMember((_g = message.guild) !== null && _g !== void 0 ? _g : null, user.id);
            profiles[user.id] = mergeProfile(profiles[user.id], buildProfile(member, user));
        }
        // threads
        if (message.thread && message.thread.lastMessage) {
            const threadLastMessage = message.thread.lastMessage;
            const member = await resolveMember((_j = (_h = threadLastMessage.guild) !== null && _h !== void 0 ? _h : message.guild) !== null && _j !== void 0 ? _j : null, threadLastMessage.author.id, threadLastMessage.member, Boolean(threadLastMessage.webhookId));
            profiles[threadLastMessage.author.id] = mergeProfile(profiles[threadLastMessage.author.id], buildProfile(member, threadLastMessage.author));
        }
    }
    for (const message of messages) {
        const author = message.author;
        const profile = profiles[author.id];
        if (!profile || !profile.bot || profile.verified)
            continue;
        if (confirmedHumans.has(author.id)) {
            profile.bot = false;
        }
    }
    return profiles;
}
function mergeProfile(existing, next) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    if (!existing) {
        return next;
    }
    const nextRoleColor = next.roleColor && next.roleColor !== '#000000' ? next.roleColor : undefined;
    const existingRoleColor = existing.roleColor && existing.roleColor !== '#000000' ? existing.roleColor : undefined;
    return {
        author: next.author || existing.author,
        avatar: (_a = next.avatar) !== null && _a !== void 0 ? _a : existing.avatar,
        roleColor: nextRoleColor !== null && nextRoleColor !== void 0 ? nextRoleColor : existingRoleColor,
        roleIcon: (_b = next.roleIcon) !== null && _b !== void 0 ? _b : existing.roleIcon,
        roleName: (_c = next.roleName) !== null && _c !== void 0 ? _c : existing.roleName,
        serverTagText: (_d = next.serverTagText) !== null && _d !== void 0 ? _d : existing.serverTagText,
        serverTagBadge: (_e = next.serverTagBadge) !== null && _e !== void 0 ? _e : existing.serverTagBadge,
        serverTagGuildId: (_f = next.serverTagGuildId) !== null && _f !== void 0 ? _f : existing.serverTagGuildId,
        bot: (_g = next.bot) !== null && _g !== void 0 ? _g : existing.bot,
        verified: (_h = next.verified) !== null && _h !== void 0 ? _h : existing.verified,
    };
}
function buildProfile(member, author) {
    var _a, _b, _c, _d, _e, _f;
    const serverTag = getServerTag(author);
    const roleIconRole = member === null || member === void 0 ? void 0 : member.roles.icon;
    const roleColor = (member === null || member === void 0 ? void 0 : member.displayHexColor) && member.displayHexColor !== '#000000' ? member.displayHexColor : undefined;
    return {
        author: (_b = (_a = member === null || member === void 0 ? void 0 : member.nickname) !== null && _a !== void 0 ? _a : author.displayName) !== null && _b !== void 0 ? _b : author.username,
        avatar: (_c = member === null || member === void 0 ? void 0 : member.displayAvatarURL({ size: 64 })) !== null && _c !== void 0 ? _c : author.displayAvatarURL({ size: 64 }),
        roleColor,
        roleIcon: getRoleIconUrl(roleIconRole),
        roleName: (roleIconRole === null || roleIconRole === void 0 ? void 0 : roleIconRole.name) || ((_d = member === null || member === void 0 ? void 0 : member.roles.hoist) === null || _d === void 0 ? void 0 : _d.name) || ((_e = member === null || member === void 0 ? void 0 : member.roles.highest) === null || _e === void 0 ? void 0 : _e.name) || undefined,
        serverTagText: serverTag === null || serverTag === void 0 ? void 0 : serverTag.text,
        serverTagBadge: serverTag === null || serverTag === void 0 ? void 0 : serverTag.badge,
        serverTagGuildId: serverTag === null || serverTag === void 0 ? void 0 : serverTag.guildId,
        bot: author.bot,
        verified: (_f = author.flags) === null || _f === void 0 ? void 0 : _f.has(discord_js_1.UserFlags.VerifiedBot),
    };
}
function getRoleIconUrl(role) {
    var _a;
    if (!role) {
        return undefined;
    }
    return ((_a = role.iconURL({ size: 32 })) !== null && _a !== void 0 ? _a : (role.unicodeEmoji
        ? (0, utils_1.parseDiscordEmoji)({
            name: role.unicodeEmoji,
        })
        : undefined));
}
function getServerTag(author) {
    var _a, _b;
    const user = author;
    const primaryGuild = user.primaryGuild;
    if (!primaryGuild || primaryGuild.identityEnabled !== true || !primaryGuild.tag) {
        return undefined;
    }
    const text = primaryGuild.tag.trim().slice(0, 4);
    if (!text) {
        return undefined;
    }
    const badge = typeof user.guildTagBadgeURL === 'function' ? ((_a = user.guildTagBadgeURL({ size: 32 })) !== null && _a !== void 0 ? _a : undefined) : undefined;
    return {
        text,
        badge,
        guildId: (_b = primaryGuild.identityGuildId) !== null && _b !== void 0 ? _b : undefined,
    };
}
//# sourceMappingURL=buildProfiles.js.map