"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.RenderType = void 0;
exports.default = MessageContent;
exports.MessageSingleASTNode = MessageSingleASTNode;
exports.getChannelType = getChannelType;
const discordComponents_1 = require("../discordComponents");
const discord_markdown_parser_1 = __importDefault(require("discord-markdown-parser"));
const discord_js_1 = require("discord.js");
const react_1 = __importDefault(require("react"));
const utils_1 = require("../../utils/utils");
var RenderType;
(function (RenderType) {
    RenderType[RenderType["EMBED"] = 0] = "EMBED";
    RenderType[RenderType["REPLY"] = 1] = "REPLY";
    RenderType[RenderType["NORMAL"] = 2] = "NORMAL";
    RenderType[RenderType["WEBHOOK"] = 3] = "WEBHOOK";
})(RenderType || (exports.RenderType = RenderType = {}));
const TIMESTAMP_LOCALE = 'en-GB';
const DISCORD_ANSI_FOREGROUND = {
    30: '#4e5058',
    31: '#dc322f',
    32: '#859900',
    33: '#b58900',
    34: '#268bd2',
    35: '#d33682',
    36: '#2aa198',
    37: '#ffffff',
    90: '#4e5058',
    91: '#dc322f',
    92: '#859900',
    93: '#b58900',
    94: '#268bd2',
    95: '#d33682',
    96: '#2aa198',
    97: '#ffffff',
};
const DISCORD_ANSI_BACKGROUND = {
    40: '#002b36',
    41: '#cb4b16',
    42: '#586e75',
    43: '#657b83',
    44: '#839496',
    45: '#6c71c4',
    46: '#93a1a1',
    47: '#fdf6e3',
    100: '#002b36',
    101: '#cb4b16',
    102: '#586e75',
    103: '#657b83',
    104: '#839496',
    105: '#6c71c4',
    106: '#93a1a1',
    107: '#fdf6e3',
};
// eslint-disable-next-line no-control-regex
const ANSI_ESCAPE_SEQUENCE = /\u001b\[([0-9;]*)m/g;
function getDefaultAnsiState() {
    return {
        backgroundColor: undefined,
        bold: false,
        color: undefined,
        underline: false,
    };
}
function parseAnsiCodes(sequence) {
    if (!sequence) {
        return [0];
    }
    return sequence
        .split(';')
        .map((part) => Number.parseInt(part, 10))
        .filter((part) => Number.isFinite(part));
}
function applyAnsiCodes(state, codes) {
    const nextState = Object.assign({}, state);
    for (const code of codes.length > 0 ? codes : [0]) {
        switch (code) {
            case 0:
                Object.assign(nextState, getDefaultAnsiState());
                break;
            case 1:
                nextState.bold = true;
                break;
            case 4:
                nextState.underline = true;
                break;
            case 22:
                nextState.bold = false;
                break;
            case 24:
                nextState.underline = false;
                break;
            case 39:
                nextState.color = undefined;
                break;
            case 49:
                nextState.backgroundColor = undefined;
                break;
            default:
                if (DISCORD_ANSI_FOREGROUND[code]) {
                    nextState.color = DISCORD_ANSI_FOREGROUND[code];
                }
                else if (DISCORD_ANSI_BACKGROUND[code]) {
                    nextState.backgroundColor = DISCORD_ANSI_BACKGROUND[code];
                }
                break;
        }
    }
    return nextState;
}
function getAnsiSegments(content) {
    var _a;
    const segments = [];
    let state = getDefaultAnsiState();
    let lastIndex = 0;
    ANSI_ESCAPE_SEQUENCE.lastIndex = 0;
    for (let match = ANSI_ESCAPE_SEQUENCE.exec(content); match !== null; match = ANSI_ESCAPE_SEQUENCE.exec(content)) {
        if (match.index > lastIndex) {
            segments.push({
                state: Object.assign({}, state),
                text: content.slice(lastIndex, match.index),
            });
        }
        state = applyAnsiCodes(state, parseAnsiCodes((_a = match[1]) !== null && _a !== void 0 ? _a : ''));
        lastIndex = match.index + match[0].length;
    }
    if (lastIndex < content.length) {
        segments.push({
            state: Object.assign({}, state),
            text: content.slice(lastIndex),
        });
    }
    return segments;
}
function getAnsiStyle(state) {
    return Object.assign(Object.assign(Object.assign(Object.assign({}, (state.backgroundColor ? { backgroundColor: state.backgroundColor } : {})), (state.bold ? { fontWeight: 700 } : {})), (state.color ? { color: state.color } : {})), (state.underline ? { textDecoration: 'underline' } : {}));
}
function DiscordAnsiCodeBlock({ content }) {
    const segments = getAnsiSegments(content);
    return (react_1.default.createElement("pre", { className: "discord-code-block-pre discord-code-block-pre--multiline discord-ansi-code-block" },
        react_1.default.createElement("code", { className: "discord-ansi-code" }, segments.map((segment, index) => segment.text.length > 0 ? (react_1.default.createElement("span", { key: index, style: getAnsiStyle(segment.state) }, segment.text)) : null))));
}
function formatTwoDigitDate(date) {
    return new Intl.DateTimeFormat(TIMESTAMP_LOCALE, {
        day: '2-digit',
        month: '2-digit',
        year: 'numeric',
    }).format(date);
}
function formatTime(date, includeSeconds = false) {
    return new Intl.DateTimeFormat(TIMESTAMP_LOCALE, Object.assign(Object.assign({ hour: '2-digit', minute: '2-digit' }, (includeSeconds ? { second: '2-digit' } : {})), { hour12: false })).format(date);
}
function formatLongDate(date, withWeekday = false) {
    return new Intl.DateTimeFormat(TIMESTAMP_LOCALE, Object.assign(Object.assign({}, (withWeekday ? { weekday: 'long' } : {})), { day: 'numeric', month: 'long', year: 'numeric' })).format(date);
}
function formatRelativeTime(target) {
    const differenceMs = target.getTime() - Date.now();
    const isFuture = differenceMs > 0;
    const absoluteMs = Math.abs(differenceMs);
    const minute = 60 * 1000;
    const hour = 60 * minute;
    const day = 24 * hour;
    const month = 30 * day;
    const year = 365 * day;
    const toLabel = (value, unit) => {
        const label = `${value} ${unit}${value === 1 ? '' : 's'}`;
        return isFuture ? `in ${label}` : `${label} ago`;
    };
    if (absoluteMs < minute) {
        const seconds = Math.max(1, Math.floor(absoluteMs / 1000));
        return toLabel(seconds, 'second');
    }
    if (absoluteMs < hour) {
        return toLabel(Math.floor(absoluteMs / minute), 'minute');
    }
    if (absoluteMs < day) {
        return toLabel(Math.floor(absoluteMs / hour), 'hour');
    }
    if (absoluteMs < month) {
        return toLabel(Math.max(1, Math.round(absoluteMs / day)), 'day');
    }
    if (absoluteMs < year) {
        return toLabel(Math.max(1, Math.round(absoluteMs / month)), 'month');
    }
    return toLabel(Math.max(1, Math.round(absoluteMs / year)), 'year');
}
function formatDiscordTimestamp(timestamp, format) {
    const date = new Date(timestamp);
    switch (format) {
        case 't':
            return formatTime(date);
        case 'T':
            return formatTime(date, true);
        case 'd':
            return formatTwoDigitDate(date);
        case 'D':
            return formatLongDate(date);
        case 'f':
            return `${formatLongDate(date)} ${formatTime(date)}`;
        case 'F':
            return `${formatLongDate(date, true)} ${formatTime(date)}`;
        case 'R':
            return formatRelativeTime(date);
        default:
            return `${formatLongDate(date, true)} ${formatTime(date)}`;
    }
}
async function MessageContent({ content, context }) {
    if (context.type === RenderType.REPLY && content.length > 180)
        content = content.slice(0, 180) + '...';
    const processedContent = content
        .replace(/^(\s{1,})- /gm, (_match, p1) => p1.replace(/ /g, '\u00A0') + '\u25E6 ')
        .replace(/^- /gm, '\u2022 ')
        .replace(/(?<!\/)(?:^|\s)(discord\.gg\/\S+)/gi, (match, invite) => match.replace(invite, `https://${invite}`));
    const parsed = (0, discord_markdown_parser_1.default)(processedContent, 'extended');
    const isOnlyEmojis = parsed.every((node) => ['emoji', 'twemoji'].includes(node.type) || (node.type === 'text' && node.content.trim().length === 0));
    if (isOnlyEmojis) {
        const emojis = parsed.filter((node) => ['emoji', 'twemoji'].includes(node.type));
        if (emojis.length <= 25) {
            context._internal = {
                largeEmojis: true,
            };
        }
    }
    return react_1.default.createElement(MessageASTNodes, { nodes: parsed, context: context });
}
async function MessageASTNodes({ nodes, context, }) {
    if (Array.isArray(nodes)) {
        return (react_1.default.createElement(react_1.default.Fragment, null, nodes.map((node, i) => (react_1.default.createElement(MessageSingleASTNode, { node: node, context: context, key: i })))));
    }
    else {
        return react_1.default.createElement(MessageSingleASTNode, { node: nodes, context: context });
    }
}
async function MessageSingleASTNode({ node, context }) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    if (!node)
        return null;
    const type = node.type;
    switch (type) {
        case 'text':
            return node.content;
        case 'link':
            return (react_1.default.createElement("a", { href: node.target },
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'url':
        case 'autolink': {
            const url = (_a = node.target) !== null && _a !== void 0 ? _a : (_c = (_b = node.content) === null || _b === void 0 ? void 0 : _b[0]) === null || _c === void 0 ? void 0 : _c.content;
            const text = node.target ? node.content : url;
            if (typeof url === 'string' && (url.includes('discord.gg/') || url.includes('discord.com/invite/'))) {
                const code = url.split('/').pop();
                if (code && context.callbacks.resolveInvite) {
                    const invite = await context.callbacks.resolveInvite(code);
                    if (invite) {
                        const inviteIcon = invite.icon &&
                            ((_d = (await context.callbacks.resolveTranscriptAssetUrl({
                                kind: 'invite-icon',
                                url: invite.icon,
                            }))) !== null && _d !== void 0 ? _d : invite.icon);
                        return (react_1.default.createElement("div", { style: { marginTop: '4px' } },
                            react_1.default.createElement("a", { href: url, target: "_blank", rel: "noreferrer", style: { color: '#00a8fc', textDecoration: 'none' } }, typeof text === 'string' ? text : react_1.default.createElement(MessageASTNodes, { nodes: text, context: context })),
                            react_1.default.createElement(discordComponents_1.DiscordInvite, { url: invite.url, name: invite.name, icon: inviteIcon !== null && inviteIcon !== void 0 ? inviteIcon : undefined, online: invite.online, members: invite.members, joinBtn: "Go to Server", inviteTitle: "YOU'VE BEEN INVITED TO JOIN A SERVER" })));
                    }
                    else {
                        return (react_1.default.createElement("div", { style: { marginTop: '4px' } },
                            react_1.default.createElement("a", { href: url, target: "_blank", rel: "noreferrer", style: { color: '#00a8fc', textDecoration: 'none' } }, typeof text === 'string' ? text : react_1.default.createElement(MessageASTNodes, { nodes: text, context: context })),
                            react_1.default.createElement("div", { className: "discord-invite", style: { marginTop: '4px' } },
                                react_1.default.createElement("div", { className: "discord-invite-header" }, "You sent an invite, but..."),
                                react_1.default.createElement("div", { style: { display: 'flex', alignItems: 'center', gap: '12px', padding: '4px 0' } },
                                    react_1.default.createElement("span", { style: { fontSize: '24px' } }, "\u2709\uFE0F"),
                                    react_1.default.createElement("div", null,
                                        react_1.default.createElement("div", { style: { color: '#f23f42', fontWeight: 700, fontSize: '16px' } }, "Invalid Invite"),
                                        react_1.default.createElement("div", { style: { color: '#b5bac1', fontSize: '14px' } }, "Try sending a new invite!"))))));
                    }
                }
            }
            return (react_1.default.createElement("a", { href: url, target: "_blank", rel: "noreferrer", style: { color: '#00a8fc', textDecoration: 'none' } }, typeof text === 'string' ? text : react_1.default.createElement(MessageASTNodes, { nodes: text, context: context })));
        }
        case 'blockQuote':
            if (context.type === RenderType.REPLY) {
                return react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context });
            }
            return (react_1.default.createElement(discordComponents_1.DiscordQuote, null,
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'br':
        case 'newline':
            if (context.type === RenderType.REPLY)
                return ' ';
            return react_1.default.createElement("br", null);
        case 'channel': {
            const id = node.id;
            const channel = await context.callbacks.resolveChannel(id);
            return (react_1.default.createElement(discordComponents_1.DiscordMention, { type: channel ? (channel.isDMBased() ? 'channel' : getChannelType(channel.type)) : 'channel' }, channel ? (channel.isDMBased() ? 'DM Channel' : channel.name) : `<#${id}>`));
        }
        case 'role': {
            const id = node.id;
            const role = await context.callbacks.resolveRole(id);
            return (react_1.default.createElement(discordComponents_1.DiscordMention, { type: "role", color: context.type === RenderType.REPLY ? undefined : role === null || role === void 0 ? void 0 : role.hexColor }, role ? role.name : `deleted-role`));
        }
        case 'user': {
            const id = node.id;
            const user = await context.callbacks.resolveUser(id);
            return react_1.default.createElement(discordComponents_1.DiscordMention, { type: "user" }, user ? ((_e = user.displayName) !== null && _e !== void 0 ? _e : user.username) : `Unknown User`);
        }
        case 'here':
        case 'everyone':
            return (react_1.default.createElement(discordComponents_1.DiscordMention, { type: 'role', highlight: true }, type));
        case 'codeBlock':
            if (context.type !== RenderType.REPLY) {
                if (((_f = node.lang) === null || _f === void 0 ? void 0 : _f.toLowerCase()) === 'ansi') {
                    return react_1.default.createElement(DiscordAnsiCodeBlock, { content: node.content });
                }
                return react_1.default.createElement(discordComponents_1.DiscordCodeBlock, { language: node.lang, code: node.content });
            }
            return react_1.default.createElement(discordComponents_1.DiscordInlineCode, null, node.content);
        case 'inlineCode':
            return react_1.default.createElement(discordComponents_1.DiscordInlineCode, null, node.content);
        case 'em':
            return (react_1.default.createElement(discordComponents_1.DiscordItalic, null,
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'strong':
            return (react_1.default.createElement(discordComponents_1.DiscordBold, null,
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'underline':
            return (react_1.default.createElement(discordComponents_1.DiscordUnderlined, null,
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'strikethrough':
            return (react_1.default.createElement("s", null,
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'emoticon':
            return typeof node.content === 'string' ? (node.content) : (react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context }));
        case 'spoiler':
            return (react_1.default.createElement(discordComponents_1.DiscordSpoiler, null,
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'emoji':
        case 'twemoji': {
            const baseEmojiUrl = (0, utils_1.parseDiscordEmoji)(node);
            const emojiUrl = (_g = (await context.callbacks.resolveTranscriptAssetUrl({
                kind: 'emoji',
                url: baseEmojiUrl,
            }))) !== null && _g !== void 0 ? _g : baseEmojiUrl;
            return (react_1.default.createElement(discordComponents_1.DiscordCustomEmoji, { name: node.name, url: emojiUrl, embedEmoji: context.type === RenderType.EMBED, largeEmoji: (_h = context._internal) === null || _h === void 0 ? void 0 : _h.largeEmojis }));
        }
        case 'timestamp':
            return (react_1.default.createElement("span", { className: "discord-time" }, formatDiscordTimestamp(parseInt(node.timestamp) * 1000, node.format)));
        case 'subtext':
            return (react_1.default.createElement("span", { style: { fontSize: '11px', color: '#B5BAC1', display: 'block', marginTop: '4px', lineHeight: 'normal' } },
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        case 'heading': {
            const level = node.level;
            const HeadingTag = `h${level}`;
            return (react_1.default.createElement(HeadingTag, { style: { margin: '8px 0', padding: 0, fontWeight: 'bold' } },
                react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context })));
        }
        case 'list': {
            const items = node.items;
            return (react_1.default.createElement(react_1.default.Fragment, null, items.map((item, i) => (react_1.default.createElement(react_1.default.Fragment, { key: i },
                react_1.default.createElement("span", null, node.ordered ? `${node.start + i}. ` : '• '),
                react_1.default.createElement(MessageASTNodes, { nodes: item, context: context }),
                i < items.length - 1 ? react_1.default.createElement("br", null) : null)))));
        }
        default: {
            console.log(`Unknown node type: ${type}`, node);
            return typeof node.content === 'string' ? (node.content) : (react_1.default.createElement(MessageASTNodes, { nodes: node.content, context: context }));
        }
    }
}
function getChannelType(channelType) {
    switch (channelType) {
        case discord_js_1.ChannelType.GuildCategory:
        case discord_js_1.ChannelType.GuildAnnouncement:
        case discord_js_1.ChannelType.GuildText:
        case discord_js_1.ChannelType.DM:
        case discord_js_1.ChannelType.GroupDM:
        case discord_js_1.ChannelType.GuildDirectory:
        case discord_js_1.ChannelType.GuildMedia:
            return 'channel';
        case discord_js_1.ChannelType.GuildVoice:
        case discord_js_1.ChannelType.GuildStageVoice:
            return 'voice';
        case discord_js_1.ChannelType.PublicThread:
        case discord_js_1.ChannelType.PrivateThread:
        case discord_js_1.ChannelType.AnnouncementThread:
            return 'thread';
        case discord_js_1.ChannelType.GuildForum:
            return 'forum';
        default:
            return 'channel';
    }
}
//# sourceMappingURL=content.js.map